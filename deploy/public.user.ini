; server/public/.user.ini としてアップロードする（ファイル名は .user.ini）
; Xserver PHP 8.3 の起動時 Deprecated 通知などを本番で抑止し、
; レスポンス本文（JSON）への混入を防ぐ。
display_errors = Off
log_errors = On
error_reporting = E_ALL & ~E_DEPRECATED & ~E_NOTICE & ~E_STRICT
